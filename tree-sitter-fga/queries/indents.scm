[
  (relations_block)
  (condition_body)
  (parameter_list)
  (direct_assignment)
  (grouping)
  (list)
  (map)
] @indent.begin

[
  ")"
  "]"
  "}"
] @indent.branch @indent.end

(type_definition) @indent.begin
(comment) @indent.auto
